<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.maather.hajj">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Hajj">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Languages" />
        <activity android:name=".arabic_nusuk" />
        <activity android:name=".progress" />
        <activity
            android:name=".features"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".journey"
            android:label="@string/title_activity_journey"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".journey1" />
        <activity android:name=".login" />
        <activity android:name=".recovery" />
        <activity android:name=".personal_information"></activity>
    </application>

</manifest>